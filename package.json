{
  "name": "custody-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "lint": "solhint 'contracts/**/*.sol'",
    "format": "prettier --write .",
    "deploy:sepolia": "hardhat run scripts/00_deploy_policy_guard.ts --network sepolia && hardhat run scripts/01_deploy_omnibus_vault.ts --network sepolia && hardhat run scripts/02_deploy_cold_vault.ts --network sepolia && hardhat run scripts/10_link_vaults.ts --network sepolia",
    "verify:sepolia": "hardhat run scripts/99_verify_all.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "ethers": "^6.12.0",
    "hardhat": "^2.22.8",
    "solhint": "^4.5.4",
    "prettier": "^3.3.2",
    "prettier-plugin-solidity": "^1.3.1",
    "typescript": "^5.4.5"
  }
}
